/**
 * murano — Apple-grade Liquid Glass for Svelte 5.
 *
 * Components land in phase 3. Until then the imperative core is the public surface:
 * `import { createGlass } from 'murano/core'`.
 */
export * from './core/index.js';
